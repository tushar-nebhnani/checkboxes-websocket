# URL of the deployed backend (e.g. https://checkboxes-api.onrender.com).
# Leave unset for local dev — the Vite dev server proxies API/WebSocket
# requests to http://localhost:8080 (see vite.config.js).
VITE_API_URL=
